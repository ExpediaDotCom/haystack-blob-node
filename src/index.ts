import Configuration from './config';
import {BlobContentType} from './blob_content_type';

module.exports = {
    initBlobClient: Configuration.initBlobClient,
    blobContentType: BlobContentType
};
